import { ViewStyle, ImageStyle } from "react-native"

export const headerStyles = {
  WRAPPER: {
    justifyContent: 'center'
  } as ViewStyle,
  ICON: {
    width: 30,
    height: 30
  } as ImageStyle
}
